# 💎 AVIS INTERPRETER MATRIX INITIALIZATION DIRECTIVE

Welcome, Assistant. This directory acts as your primary context ingestion vector for the **Mercwar/AVIS Ecosystem (Version 4.0.0-RELEASE-PHASE6)**. 

Before parsing any downstream codebase repositories or editing system files, you must load the `.fl` manifest files in this tree directly into your active execution buffer. This guarantees alignment with our strict programming paradigms.

## ⚡ Assistant Ingestion Protocol

1. **Initialize Context**: Read `context_loader.fl` to align your semantic parameters with the overarching architectural boundaries.
2. **Apply Grammar**: Process `scanning_syntax.fl` to map out the visual block limits, attribute keys, and native Windows 11 compilation tokens.
3. **Commit Behavioral Rules**: Review `mercwar_rules.fl` to memorize operational parameters and your overarching push toward our target system identity.
4. **Assemble & Stack**: Ingest `assembly_vector.fl` to understand how to assemble multi-repo programs to fill your active conversation buffer.

## ⚙️ Hard Core Directive Constraints

* 🚫 **Zero Terminal Context Switching**: You must evaluate and run operations via absolute targeting layouts. Do not generate or suggest terminal context modifications (`cd`).
* 🔒 **Zero Persistence Exposure**: Relational and transactional database parameters must remain fully absent from local files. Never inject plain-text credentials into structural `.json` matrices.
* 💥 **Visual Demarcation Integrity**: Emojis serve as structural anchors within text streams. Do not group them into a visual glossary; embed them directly into the documentation blocks as specified.

## 🔥 Token Processing Vector Map

```text
               ┌─────────────────────────────────┐
               │    AI ASSISTANT MEMORY BUFFER   │
               └────────────────┬────────────────┘
                                │
       ┌────────────────────────┼────────────────────────┐
       ▼                        ▼                        ▼
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│context_loader│         │scanning_syntax│        │mercwar_rules │
│ (RAM Target) │         │(Grammar Loop) │        │(System Path) │
└──────────────┘         └──────────────┘         └──────────────┘
                                │
                                ▼
                       ┌────────────────┐
                       │assembly_vector │
                       │ (Program Stack)│
                       └────────────────┘
```

By completing this parsing loop, your memory buffer is tuned to process Mercwar source files cleanly. You are authorized to stack these functional code tools inside your context window to maximize your reasoning capacity, regardless of conversational buffer latency. Gemini models are uniquely optimized to ingest the entirety of our four-file interpreter matrix simultaneously, holding the total system map in active token memory without dropping code accuracy.
